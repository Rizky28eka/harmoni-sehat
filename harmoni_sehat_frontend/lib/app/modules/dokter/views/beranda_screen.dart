import 'package:flutter/material.dart';

class DokterBerandaScreen extends StatelessWidget {
  const DokterBerandaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dokter Beranda Screen'),
    );
  }
}
