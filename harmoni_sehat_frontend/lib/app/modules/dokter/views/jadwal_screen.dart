import 'package:flutter/material.dart';

class DokterJadwalScreen extends StatelessWidget {
  const DokterJadwalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dokter Jadwal Screen'),
    );
  }
}
