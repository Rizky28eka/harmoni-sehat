import 'package:flutter/material.dart';

class DokterChatScreen extends StatelessWidget {
  const DokterChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dokter Chat Screen'),
    );
  }
}
