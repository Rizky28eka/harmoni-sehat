import 'package:flutter/material.dart';

class PasienJadwalScreen extends StatelessWidget {
  const PasienJadwalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pasien Jadwal Screen'),
    );
  }
}
