import 'package:flutter/material.dart';

class PasienProfilScreen extends StatelessWidget {
  const PasienProfilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pasien Profil Screen'),
    );
  }
}
