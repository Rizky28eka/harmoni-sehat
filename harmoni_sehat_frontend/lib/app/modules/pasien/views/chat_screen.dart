import 'package:flutter/material.dart';

class PasienChatScreen extends StatelessWidget {
  const PasienChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pasien Chat Screen'),
    );
  }
}
