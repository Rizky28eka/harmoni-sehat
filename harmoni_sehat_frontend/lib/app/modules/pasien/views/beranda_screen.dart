import 'package:flutter/material.dart';

class PasienBerandaScreen extends StatelessWidget {
  const PasienBerandaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pasien Beranda Screen'),
    );
  }
}
