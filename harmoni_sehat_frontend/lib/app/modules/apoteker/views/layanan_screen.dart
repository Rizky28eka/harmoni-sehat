import 'package:flutter/material.dart';

class ApotekerLayananScreen extends StatelessWidget {
  const ApotekerLayananScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Apoteker Layanan Screen'),
    );
  }
}
