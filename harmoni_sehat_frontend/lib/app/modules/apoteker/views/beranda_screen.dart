import 'package:flutter/material.dart';

class ApotekerBerandaScreen extends StatelessWidget {
  const ApotekerBerandaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Apoteker Beranda Screen'),
    );
  }
}
