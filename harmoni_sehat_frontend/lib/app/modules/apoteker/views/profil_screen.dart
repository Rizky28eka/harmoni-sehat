import 'package:flutter/material.dart';

class ApotekerProfilScreen extends StatelessWidget {
  const ApotekerProfilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Apoteker Profil Screen'),
    );
  }
}
